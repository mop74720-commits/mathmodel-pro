# Changelog

## v0.3.0

这不是“补齐上游文件”，而是对已有重叠能力做逐项择优。

- 保留 v0.2 的 coach / skill-router / competition-repo 三层边界。
- 对 ll2010650 上游与本仓库重叠的审题、建模、实现、验证、写作内容逐项融合。
- `modeling-norms.md` 从极简清单升级为通用建模纪律，但删除机械算法决策树与固定阈值。
- `award-winning-papers.md` 和 `training-papers-lessons.md` 采用“证据丰富但压缩为通用模式”的折中。
- `word-paper-pipeline.md` 改成环境中立的 Word / LaTeX 双轨生产线。
- 对 chenboshuo/cumcm_template 的重叠实现完成评估后，保留现有 `main.tex`、Makefile、PowerShell 和 sections，因为现有版本更安全、更通用、对当前官方格式更中立。
- 新增 `UPSTREAM_COMPARISON.md`，记录每个重叠项的选择理由。

## v0.2.0

- 建立比赛 coach 定位。
- 增加 playbooks 与 competition-repo 模板。
