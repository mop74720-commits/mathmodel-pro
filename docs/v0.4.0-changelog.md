# v0.4.0 changelog

- 增加 Selection Workspace：探索候选题/路线时允许未成熟假设，但不是正式事实源。
- route-led selection + information-gain cheap probe。
- 增加 strongest objection / deciding evidence / refutation / flip condition / fallback trigger-action。
- 不采用固定 45/55 权重、固定分差、固定 Day-One gate。
- 只晋升有证据价值的 selection 内容；不得自动写入 FACTS。
- route-level blocker 可重开 selection；局部失败仍交 SkillHub。
- 保留 MIT attribution。
