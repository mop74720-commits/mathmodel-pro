# v0.4.1 Quality Repair

- 将最终竞赛成果质量明确置于系统闭环、审计数量和文件完整度之上。
- 区分 `REPRODUCIBLE` 与 `COMPETITIVE_ENOUGH`：可靠结果不自动等于值得冻结。
- 冻结前只在存在具体、可信、高信息量的 incumbent challenge 时追加挑战；不规定固定模型数或搜索次数。
- Decision Log 建议区分 `validated / reproducible / competitiveness_remaining / freeze_reason`。
- 保留 v0.4.0 Selection Workspace、官方规则闭环和 State-first 架构。
