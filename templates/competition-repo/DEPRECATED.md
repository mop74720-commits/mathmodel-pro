# DEPRECATED

Do not initialize a real competition from this directory.

Use the independent `competition-template` package (v0.1.1 or later) as the single authoritative Competition Repo template.

This stub exists only so older prompts that mention `templates/competition-repo` fail visibly instead of silently creating the obsolete q1–q4-fixed layout.
